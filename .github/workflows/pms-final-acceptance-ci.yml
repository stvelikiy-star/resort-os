name: PMS Final Acceptance CI

on:
  pull_request:
    branches:
      - main
      - integration/site-pms-cms-20260827
    paths:
      - 'services/api/**'
      - 'packages/database/**'
      - 'data-intake/**'
      - 'scripts/verify_pms_resize_financial_invariants.py'
      - '.github/workflows/pms-final-acceptance-ci.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pms-final-acceptance:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: resort_os
          POSTGRES_USER: resort
          POSTGRES_PASSWORD: resort
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U resort -d resort_os"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      DATABASE_URL: postgresql://resort:resort@localhost:5432/resort_os?schema=public
      PROPERTY_CODE: THREE_CROWNS
      RATE_PLAN_CODE: DIRECT_2026_27
      BOOTSTRAP_OWNER_USERNAME: ci-owner
      BOOTSTRAP_OWNER_PASSWORD: CI-Only-Strong-Password-2026
      BOOTSTRAP_OWNER_DISPLAY_NAME: CI Owner
      COOKIE_SECURE: false
      CORE_API_URL: http://127.0.0.1:8000

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Build clean PostgreSQL schema
        working-directory: packages/database
        run: |
          npm install --no-audit --no-fund
          npx prisma validate
          npx prisma db push

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: services/api/requirements.txt

      - name: Install Core and seed approved 84-room intake
        run: |
          pip install -r services/api/requirements.txt
          python -m compileall services/api/app scripts/verify_pms_resize_financial_invariants.py
          python scripts/apply_core_constraints.py
          python scripts/seed_from_intake.py
          python scripts/bootstrap_owner.py

      - name: Start Resort Core
        run: |
          python -m uvicorn app.app_entry:app --app-dir services/api --host 127.0.0.1 --port 8000 > /tmp/pms-final-api.log 2>&1 &
          echo $! > /tmp/pms-final-api.pid
          for i in {1..45}; do
            curl --fail --silent http://127.0.0.1:8000/health >/dev/null && exit 0
            sleep 1
          done
          cat /tmp/pms-final-api.log || true
          exit 1

      - name: E2E — resize pricing preview and immutable Payment fact
        run: python scripts/verify_pms_resize_financial_invariants.py

      - name: Show Core diagnostics on failure
        if: failure()
        run: cat /tmp/pms-final-api.log || true

      - name: Stop Core
        if: always()
        run: |
          if [ -f /tmp/pms-final-api.pid ]; then kill "$(cat /tmp/pms-final-api.pid)" 2>/dev/null || true; fi
